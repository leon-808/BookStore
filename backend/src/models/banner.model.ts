export interface Banner {
  id: number; // book.id
  title: string; // book.title
  description: string; // book.summary
  image: string; // book.img
  url: string; // http://localhost:3000/books/book.id
  target: string;
}
